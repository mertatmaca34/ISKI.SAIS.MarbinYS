<Page
    x:Class="WinUI.Views.CalibrationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:WinUI.Controls"
    xmlns:models="using:WinUI.Models"
    mc:Ignorable="d">

    <Grid Padding="24" RowDefinitions="Auto,Auto,*" RowSpacing="16">
        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBlock Text="Kalibrasyon" Style="{StaticResource PageTitleTextStyle}" />
            <controls:BusyIndicator IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <Button Content="Limitleri Yükle" Command="{x:Bind ViewModel.LoadLimitsCommand}" />
            <Button Content="Ölçüm Gönder" Command="{x:Bind ViewModel.SendMeasurementCommand}" />
        </StackPanel>

        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Limits, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:CalibrationLimitDto">
                    <StackPanel Spacing="4">
                        <TextBlock Text="{x:Bind Parameter}" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Bind ZeroRef, Mode=OneWay, StringFormat='Zero: {0}'}" />
                        <TextBlock Text="{x:Bind SpanRef, Mode=OneWay, StringFormat='Span: {0}'}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
